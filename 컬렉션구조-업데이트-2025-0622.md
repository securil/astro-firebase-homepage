# Firebase 컬렉션 구조 업데이트

**업데이트 일자**: 2025년 6월 22일  
**추가 내용**: 홀별 상세 기록을 위한 새로운 컬렉션

---

## 🔹 기존 컬렉션 (변경 없음)

### 1. `members` (회원 목록)
| 필드명       | 타입     | 설명 |
|--------------|----------|------|
| `id`         | `number` | 문서 ID 기준 값 (기존 기준) |
| `memberId`   | `number` | `id`와 동일, 연동 시 통일성 확보용 |
| `name`       | `string` | 회원 이름 |
| `generation` | `string` | 기수 (예: "47기") |
| `gender`     | `string` | 성별 (예: "남", "여") |
| `phone`      | `string` | 연락처 (010-xxxx-xxxx 형식) |

### 2. `meetings` (모임 목록)
| 필드명       | 타입     | 설명 |
|--------------|----------|------|
| `id`         | `number` | 모임 고유 ID (예: 2305) |
| `date`       | `string` | 날짜 (`YYYY-MM-DD`) |
| `location`   | `string` | 장소 또는 코스 |
| `title`      | `string` | 모임 명칭 또는 설명 |

### 3. `scores` (회원별 개별 성적)
**문서 ID 형식**: `meetingId_memberId`
| 필드명        | 타입     | 설명 |
|---------------|----------|------|
| `meetingId`   | `number` | 모임 ID |
| `memberId`    | `number` | 회원 ID |
| `gross_score` | `number` | 실제 총 타수 |

### 4. `Meeting_Awards` (수상자 기록)
**문서 ID 형식**: `meetingId_memberId_category`
| 필드명       | 타입     | 설명 |
|--------------|----------|------|
| `meetingId`  | `number` | 모임 ID |
| `memberId`   | `number` | 수상자 ID |
| `category`   | `string` | 수상 항목 (예: `"신페리오"`, `"롱기스트"`) |
| `score`      | `number` | 평가 점수 (단위 제외, 프론트에서 붙임) |

### 5. `Members_Score` (회원별 스코어 이력)
**문서 ID 형식**: `memberId`
```json
{
  "memberId": 319,
  "scores": [
    {
      "meetingId": 2305,
      "meetingDate": "2024-03-15", 
      "gross_score": 88
    }
  ]
}
```

### 6. `Meeting_Stats` (모임별 통계 정보)
| 필드명              | 타입     | 설명 |
|--------------------|----------|------|
| `meetingId`        | `number` | 모임 ID |
| `averageScore`     | `number` | 평균 타수 |
| `participantCount` | `number` | 참석자 수 |
| `date`            | `string` | 날짜 (`YYYY-MM-DD`) |

---

## 🆕 신규 컬렉션 (2025년 홀별 기록용)

### 7. `hole_scores` (홀별 상세 기록)

**문서 ID 형식**: `meetingId_memberId`

**전체 구조 예시**:
```json
{
  "meetingId": 2501,
  "memberId": 319,
  "recordDate": "2025-01-15",
  "courseInfo": {
    "name": "○○ 컨트리클럽",
    "teeBox": "백티",
    "weather": "맑음",
    "courseRating": 72.5,
    "slopeRating": 126
  },
  "holes": [
    {
      "hole": 1,
      "par": 4,
      "distance": 380,
      "score": 5,
      "putts": 2,
      "fairwayHit": true,
      "greenInRegulation": false,
      "penalties": 0,
      "sandSaves": 0,
      "upAndDown": false,
      "notes": "드라이버 우측 러프"
    },
    {
      "hole": 2,
      "par": 3,
      "distance": 165,
      "score": 3,
      "putts": 2,
      "fairwayHit": null,
      "greenInRegulation": true,
      "penalties": 0,
      "sandSaves": 0,
      "upAndDown": false,
      "notes": ""
    }
    // ... 18홀까지
  ],
  "summary": {
    "totalScore": 85,
    "frontNine": 42,
    "backNine": 43,
    "totalPutts": 32,
    "fairwaysHit": 8,
    "fairwaysPossible": 14,
    "greensInRegulation": 6,
    "greensInRegulationPossible": 18,
    "totalPenalties": 2,
    "sandSaves": 1,
    "sandSavesPossible": 3,
    "upAndDowns": 4,
    "upAndDownsPossible": 12,
    "eagles": 0,
    "birdies": 2,
    "pars": 8,
    "bogeys": 6,
    "doubleBogeys": 2,
    "others": 0
  }
}
```

**필드 상세 설명**:

#### 메타 정보
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `meetingId` | `number` | 모임 ID |
| `memberId` | `number` | 회원 ID |
| `recordDate` | `string` | 기록 날짜 (YYYY-MM-DD) |

#### 코스 정보 (`courseInfo`)
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `name` | `string` | 골프장 이름 |
| `teeBox` | `string` | 티박스 구분 (백티/블루티/화이트티 등) |
| `weather` | `string` | 날씨 상태 |
| `courseRating` | `number` | 코스 레이팅 (옵션) |
| `slopeRating` | `number` | 슬로프 레이팅 (옵션) |

#### 홀별 기록 (`holes` 배열)
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `hole` | `number` | 홀 번호 (1-18) |
| `par` | `number` | 파 (3, 4, 5) |
| `distance` | `number` | 거리 (야드) |
| `score` | `number` | 타수 |
| `putts` | `number` | 퍼트 수 |
| `fairwayHit` | `boolean\|null` | 페어웨이 적중 (Par3는 null) |
| `greenInRegulation` | `boolean` | 그린 적중 |
| `penalties` | `number` | 페널티 타수 |
| `sandSaves` | `number` | 샌드세이브 성공 여부 |
| `upAndDown` | `boolean` | 업앤다운 성공 여부 |
| `notes` | `string` | 특이사항 메모 |

#### 요약 통계 (`summary`)
| 필드명 | 타입 | 설명 |
|--------|------|------|
| `totalScore` | `number` | 총 타수 |
| `frontNine` | `number` | 전반 9홀 타수 |
| `backNine` | `number` | 후반 9홀 타수 |
| `totalPutts` | `number` | 총 퍼트 수 |
| `fairwaysHit` | `number` | 페어웨이 적중 수 |
| `fairwaysPossible` | `number` | 페어웨이 적중 가능 수 (Par3 제외) |
| `greensInRegulation` | `number` | 그린 적중 수 |
| `greensInRegulationPossible` | `number` | 그린 적중 가능 수 (18) |
| `totalPenalties` | `number` | 총 페널티 수 |
| `sandSaves` | `number` | 샌드세이브 성공 수 |
| `sandSavesPossible` | `number` | 샌드세이브 기회 수 |
| `upAndDowns` | `number` | 업앤다운 성공 수 |
| `upAndDownsPossible` | `number` | 업앤다운 기회 수 |
| `eagles` | `number` | 이글 수 |
| `birdies` | `number` | 버디 수 |
| `pars` | `number` | 파 수 |
| `bogeys` | `number` | 보기 수 |
| `doubleBogeys` | `number` | 더블보기 수 |
| `others` | `number` | 트리플보기 이상 |

---

### 8. `hole_statistics` (홀별 집계 통계)

**문서 ID 형식**: `courseId_holeNumber` 또는 `global_holeNumber`

```json
{
  "courseId": "global",
  "holeNumber": 1,
  "totalRounds": 145,
  "averageScore": 4.2,
  "par": 4,
  "averageDistance": 380,
  "scoreDistribution": {
    "1": 0,
    "2": 0, 
    "3": 5,
    "4": 68,
    "5": 52,
    "6": 18,
    "7": 2,
    "8": 0
  },
  "fairwayHitRate": 0.72,
  "greenInRegulationRate": 0.45,
  "averagePutts": 1.8,
  "difficulty": 6.2,
  "lastUpdated": "2025-06-22"
}
```

---

### 9. `personal_hole_stats` (개인별 홀 통계)

**문서 ID 형식**: `memberId_holeNumber`

```json
{
  "memberId": 319,
  "holeNumber": 1,
  "totalRounds": 12,
  "averageScore": 4.1,
  "bestScore": 3,
  "worstScore": 6,
  "parRate": 0.67,
  "birdieRate": 0.17,
  "bogeyRate": 0.16,
  "fairwayHitRate": 0.75,
  "greenInRegulationRate": 0.50,
  "averagePutts": 1.9,
  "improvementTrend": 0.15,
  "lastUpdated": "2025-06-22"
}
```

---

## 🔄 기존 컬렉션과의 연동

### 데이터 정합성 보장
1. **`hole_scores.summary.totalScore`** ↔ **`scores.gross_score`** 일치
2. **`hole_scores.meetingId`** ↔ **모든 관련 컬렉션의 meetingId** 연동
3. **`hole_scores.memberId`** ↔ **모든 관련 컬렉션의 memberId** 연동

### 집계 데이터 업데이트 트리거
- `hole_scores` 데이터 입력 시 `hole_statistics`, `personal_hole_stats` 자동 업데이트
- Firebase Functions 또는 클라이언트 측 집계 로직 구현

---

## 📈 인덱스 설정 권장사항

### `hole_scores` 컬렉션
```javascript
// 모임별 조회
meetingId (ASC)

// 회원별 조회  
memberId (ASC), recordDate (DESC)

// 날짜별 조회
recordDate (DESC)

// 복합 조회
meetingId (ASC), memberId (ASC)
```

### `hole_statistics` 컬렉션
```javascript
// 홀별 조회
holeNumber (ASC)

// 난이도별 정렬
difficulty (DESC)
```

### `personal_hole_stats` 컬렉션
```javascript
// 회원별 조회
memberId (ASC), holeNumber (ASC)

// 성과별 정렬
memberId (ASC), averageScore (ASC)
```

---

## 🚀 마이그레이션 계획

### 단계별 구현
1. **Phase 1**: `hole_scores` 컬렉션 생성 및 샘플 데이터 입력
2. **Phase 2**: 집계 컬렉션 (`hole_statistics`, `personal_hole_stats`) 생성
3. **Phase 3**: 기존 데이터와 연동 검증
4. **Phase 4**: 실제 데이터 마이그레이션 및 운영

### 데이터 검증 방법
- 총 타수 일치성 검증 스크립트 작성
- 집계 데이터 정확성 검증 로직 구현
- 데이터 백업 및 복구 절차 수립

---

**문서 관리**  
- **최초 작성**: 2025년 4월 9일  
- **최근 업데이트**: 2025년 6월 22일 (홀별 기록 컬렉션 추가)  
- **다음 리뷰**: 홀별 통계 기능 구현 완료 후
