# 월별 리포트 작성 기획서

**작성일**: 2025년 6월 28일  
**프로젝트**: 청구회 골프 동호회 웹사이트  
**목적**: 월별 모임 리포트 자동 생성 및 관리 체계 구축

---

## 📋 1. 개요

### 1.1 목적
- 매월 청구회 골프 모임 결과를 웹사이트 리포트로 자동 생성
- 스포츠 중계 스타일의 생생한 경기 분석 제공
- 홀별 상세 데이터를 활용한 심층 분석
- 일관성 있는 리포트 형식으로 브랜드 통일성 확보

### 1.2 제공받는 기본 데이터
1. **참가자 스코어 성적** (전/후반 홀별 스코어)
2. **조편성** (코스별, 시간대별 팀 구성)
3. **시상내역** (각종 시상 수상자 및 기록)

### 1.3 생성 결과물
- 웹사이트 리포트 페이지 (`/reports/YYYY-MM`)
- 갤러리 이미지 자동 배치
- 스포츠 뉴스 스타일 경기 분석 섹션

---

## 🗂️ 2. 데이터 구조 설계

### 2.1 입력 데이터 표준화

#### 홀별 스코어 데이터 (CSV → JSON)
```json
{
  "name": "홍길동",
  "generation": "30기",
  "course": "올림프스/록키", // 전반코스/후반코스
  "front": 39, // 전반 9홀 총타수 (파36 + 오버/언더파)
  "back": 37,  // 후반 9홀 총타수
  "totalScore": 76,
  "holes": {
    "front": [0,1,1,2,0,1,1,1,0], // 파 대비 스코어 (선택사항)
    "back": [1,0,1,1,1,0,1,1,1]   // 파 대비 스코어 (선택사항)
  }
}
```

#### 조편성 정보 (이미지/텍스트 → JSON)
```json
{
  "title": "청구회 월례회 조편성 청구회(XXX회) YYYY년 MM월 DD일",
  "courses": [
    {
      "name": "몽블랑",
      "startTime": "12:29-12:44",
      "teams": [
        {
          "number": 1,
          "time": "12:29",
          "players": ["이름(기수)", "이름(기수)", ...]
        }
      ]
    }
  ]
}
```

#### 시상 정보
```json
{
  "awards": [
    {
      "category": "신페리오우승",
      "name": "홍길동",
      "generation": "30기", 
      "score": "HD6, 점수 73",
      "color": "from-emerald-400 to-teal-500"
    }
  ]
}
```

### 2.2 최종 리포트 데이터 구조
```json
{
  "title": "2025년 X월 정기모임 리포트",
  "date": "YYYY-MM-DD",
  "location": "골프장명",
  "participants": 48,
  "avgScore": 87.4,
  "bestScore": 74,
  "underPar": 0,
  "eighties": 27,
  "weather": "맑음",
  "temperature": "28°C",
  "gallery": [...], // 갤러리 이미지 목록
  "awards": [...], // 시상 내역
  "courseStats": [...], // 코스별 통계 (자동 계산)
  "scores": [...], // 전체 성적표
  "femaleScores": [...], // 여성 성적표 (있는 경우)
  "teamList": {...}, // 조편성 정보
  "matchReport": {...} // 경기 리포트 (자동 생성)
}
```

---

## 📊 3. 리포트 섹션 구성

### 3.1 Hero Banner
**내용**: 
- 리포트 제목, 날짜, 장소, 참가자 수, 날씨 정보
- 배경 이미지 + 그라데이션 오버레이

**데이터 소스**: 
- 기본 정보 (수동 입력)
- 참가자 수 (성적표에서 자동 계산)

### 3.2 시상 내역 (Awards Section)
**내용**: 
- 각 시상별 수상자 카드 (4열 그리드)
- 시상별 고유 그라데이션 색상
- 호버 애니메이션

**데이터 소스**: 
- 시상 정보 (제공받은 데이터)

### 3.3 조편성표 (Team List Section)
**내용**: 
- 월례회형: 좌우 2열 레이아웃
- 대회형: 시상 리스트 + 대회 진표 + 코스별 조편성

**데이터 소스**: 
- 조편성 정보 (제공받은 데이터를 JSON으로 변환)

**표시 조건**:
- 월례회형: `data.teamList && data.teamList.title`
- 대회형: `data.teamList && data.teamList.prizes`

### 3.4 코스별 분석 (Course Stats Section)
**내용**: 
- 각 코스별 참가자 수 + 상위 3위 성적
- 4열 카드 그리드

**데이터 계산 로직**:
```javascript
// 코스별 참가자 분석 (홀별 기반)
courseAnalysis = {};
scores.forEach(player => {
  const [frontCourse, backCourse] = player.course.split('/');
  
  // 전반 코스에 전반 타수 기록
  courseAnalysis[frontCourse].front.push({
    name: player.name,
    generation: player.generation,
    score: player.front
  });
  
  // 후반 코스에 후반 타수 기록  
  courseAnalysis[backCourse].back.push({
    name: player.name,
    generation: player.generation,
    score: player.back
  });
});

// 각 코스별 상위 3명 추출
courseStats = courses.map(courseName => {
  const allPlayers = [
    ...courseAnalysis[courseName].front,
    ...courseAnalysis[courseName].back
  ];
  const top3 = allPlayers.sort((a,b) => a.score - b.score).slice(0,3);
  
  return {
    name: courseName,
    players: allPlayers.length,
    topPlayers: top3
  };
});
```

### 3.5 경기 리포트 (Match Report Section) ⭐ 신규
**내용**: 
- 헤드라인 뉴스
- 5개 스토리 카드 (우승자, 준우승자, 코스별 지배자, 난이도 분석, 깜짝 포인트)
- 통계 요약

**데이터 생성 로직**:
```javascript
// 1. 우승자 분석
const winner = scores[0];
const winnerCourses = winner.course.split('/');
const winnerFrontRank = courseAnalysis[winnerCourses[0]].front
  .sort((a,b) => a.score - b.score)
  .findIndex(p => p.name === winner.name) + 1;

// 2. 코스별 난이도 계산
const courseDifficulty = courseStats.map(course => ({
  name: course.name,
  avgTop3: course.topPlayers.reduce((sum, p) => sum + p.score, 0) / 3
})).sort((a,b) => a.avgTop3 - b.avgTop3);

// 3. 스토리 자동 생성
const matchReport = {
  headline: `${winner.name}, ${winnerCourses[1]} 코스 폭발로 ${winner.score}타 우승!`,
  stories: [
    // 우승자, 준우승자, 코스별 강자 등 스토리 생성
  ]
};
```

### 3.6 전체 성적표 (Score Table)
**내용**: 
- 순위별 상세 결과 (순위, 이름, 기수, 총타수, 코스, 전반/후반)
- 1-3위 메달 아이콘
- 파 대비 색상 코딩

**데이터 소스**: 
- 성적 데이터 (제공받은 데이터를 순위별 정렬)

### 3.7 여성 성적표 (Female Score Table)
**내용**: 
- 여성 참가자 별도 순위
- 핑크 테마

**데이터 필터링**:
```javascript
const femaleScores = scores
  .filter(player => player.generation.includes('여'))
  .map((player, index) => ({ ...player, rank: index + 1 }));
```

### 3.8 통계 대시보드 (Stats Dashboard)
**내용**: 
- 4개 카드 (최고성적/평균타수/언더파/80대)
- 그라데이션 배경

**데이터 계산**:
```javascript
const stats = {
  bestScore: Math.min(...scores.map(s => s.score)),
  avgScore: scores.reduce((sum, s) => sum + s.score, 0) / scores.length,
  underPar: scores.filter(s => s.score < 72).length,
  eighties: scores.filter(s => s.score >= 80 && s.score < 90).length
};
```

### 3.9 성과 분석 (Performance Analysis)
**내용**: 
- TOP 3 우수 성적자
- 타수 분포 분석 (프로그레스 바)

**데이터 계산**:
```javascript
const scoreDistribution = [
  { range: '70대', count: scores.filter(s => s.score >= 70 && s.score < 80).length },
  { range: '80대', count: scores.filter(s => s.score >= 80 && s.score < 90).length },
  { range: '90대', count: scores.filter(s => s.score >= 90 && s.score < 100).length },
  { range: '100대+', count: scores.filter(s => s.score >= 100).length }
];
```

### 3.10 갤러리 섹션 (Gallery Section)
**내용**: 
- 모임 사진 슬라이더
- 이미지 최적화 및 지연 로딩

**데이터 소스**: 
- `public/images/reports/YYYY-MM/gallery/` 폴더의 이미지 목록

---

## 🔄 4. 워크플로우 단계

### Step 1: 데이터 수집 및 검증
1. **홀별 스코어 처리**
   - Excel/CSV 파일을 JSON으로 변환
   - 전반/후반 타수 = 파36 + 오버/언더파 계산
   - 데이터 무결성 검사 (전반+후반 = 총타수)

2. **조편성 정보 구조화**
   - 이미지 OCR 또는 수동 입력으로 텍스트 추출
   - 참가자 명단 및 기수 정보 파싱
   - JSON 형식으로 변환

3. **시상 정보 입력**
   - 시상 카테고리별 수상자 정리
   - 색상 테마 배정

### Step 2: 자동 데이터 분석 및 집계
1. **참가자 통계**
   - 총 참가자 수, 성별 분포
   - 코스별 참가자 수 (전반/후반 구분)

2. **성적 분석**
   - 전체 평균 타수, 최저 타수
   - 언더파/80대 인원 계산
   - 타수 분포 분석

3. **코스별 통계 생성**
   - 각 코스별 전반/후반 사용자 통합
   - 코스별 상위 3명 추출
   - 난이도 분석 (상위 3명 평균)

4. **경기 리포트 자동 생성**
   - 우승자/준우승자 스토리 분석
   - 코스별 강자 및 특이 사항 추출
   - 스포츠 중계 스타일 문구 생성

### Step 3: 페이지 데이터 생성
1. **reportData 객체 완성**
   - 모든 섹션 데이터 통합
   - 필수 필드 검증

2. **파일 시스템 업데이트**
   - `getStaticPaths`에 새 period 추가
   - `YYYY-MM.json` 파일 생성
   - 갤러리 이미지 복사

### Step 4: 검증 및 배포
1. **데이터 검증 체크리스트**
   - [ ] 참가자 수 일치 (조편성 vs 성적표)
   - [ ] 코스별 참가자 분배 정확성
   - [ ] 홀별 스코어 계산 정확성 (36 + 오버파)
   - [ ] 시상자 정보 일치
   - [ ] 갤러리 이미지 표시 확인

2. **빌드 및 배포**
   ```bash
   npm run build
   npx gh-pages -d dist
   ```

---

## 🛠️ 5. 자동화 도구 설계

### 5.1 데이터 변환 스크립트 구조
```javascript
// tools/generate-report.js
class ReportGenerator {
  constructor(period) {
    this.period = period; // 'YYYY-MM'
    this.rawData = {};
    this.processedData = {};
  }

  // Step 1: 입력 데이터 로드
  loadInputData() {
    this.rawData.scores = this.loadScoreData();
    this.rawData.teamList = this.loadTeamData();
    this.rawData.awards = this.loadAwardsData();
    this.rawData.gallery = this.loadGalleryImages();
  }

  // Step 2: 홀별 데이터 기반 분석
  analyzeHoleData() {
    this.processedData.courseStats = this.calculateCourseStats();
    this.processedData.stats = this.calculateOverallStats();
    this.processedData.scoreDistribution = this.calculateScoreDistribution();
  }

  // Step 3: 경기 리포트 생성
  generateMatchReport() {
    this.processedData.matchReport = {
      headline: this.generateHeadline(),
      subheadline: this.generateSubheadline(),
      stories: this.generateStories(),
      stats: this.generateMatchStats()
    };
  }

  // Step 4: 최종 리포트 데이터 생성
  generateFinalReport() {
    return {
      ...this.rawData,
      ...this.processedData,
      title: `${this.period.split('-')[0]}년 ${parseInt(this.period.split('-')[1])}월 정기모임 리포트`,
      date: this.extractMeetingDate(),
      participants: this.rawData.scores.length
    };
  }
}
```

### 5.2 핵심 계산 함수
```javascript
// 코스별 통계 계산
calculateCourseStats() {
  const courseAnalysis = {};
  
  this.rawData.scores.forEach(player => {
    const [frontCourse, backCourse] = player.course.split('/');
    
    // 전반/후반 코스별 데이터 분리
    if (!courseAnalysis[frontCourse]) courseAnalysis[frontCourse] = {front: [], back: []};
    if (!courseAnalysis[backCourse]) courseAnalysis[backCourse] = {front: [], back: []};
    
    courseAnalysis[frontCourse].front.push({...player, score: player.front});
    courseAnalysis[backCourse].back.push({...player, score: player.back});
  });
  
  // 각 코스별 상위 3명 추출
  return Object.keys(courseAnalysis).map(courseName => {
    const allPlayers = [
      ...courseAnalysis[courseName].front,
      ...courseAnalysis[courseName].back
    ];
    const topPlayers = allPlayers
      .sort((a, b) => a.score - b.score)
      .slice(0, 3)
      .map(p => ({name: p.name, score: p.score, generation: p.generation}));
    
    return {
      name: courseName,
      players: allPlayers.length,
      topPlayers
    };
  });
}

// 경기 리포트 스토리 생성
generateStories() {
  const winner = this.rawData.scores[0];
  const runnerUp = this.rawData.scores[1];
  const courseStats = this.processedData.courseStats;
  
  return [
    {
      type: 'winner',
      icon: '🏆',
      title: '우승자 스토리',
      content: this.generateWinnerStory(winner),
      highlight: this.getWinnerHighlight(winner)
    },
    {
      type: 'runner-up',
      icon: '🥈', 
      title: '준우승자 분석',
      content: this.generateRunnerUpStory(runnerUp),
      highlight: this.getRunnerUpHighlight(runnerUp)
    },
    // ... 추가 스토리들
  ];
}
```

---

## 📁 6. 폴더 구조

```
project/
├── tools/
│   ├── generate-report.js          # 메인 생성 스크립트
│   ├── data-processors/
│   │   ├── score-processor.js      # 성적 데이터 처리
│   │   ├── team-processor.js       # 조편성 데이터 처리
│   │   └── match-analyzer.js       # 경기 분석 엔진
│   ├── data/
│   │   └── YYYY-MM/
│   │       ├── scores.csv          # 성적 데이터
│   │       ├── team-list.json      # 조편성 정보
│   │       ├── awards.json         # 시상 정보
│   │       └── gallery/            # 갤러리 이미지들
│   └── templates/
│       └── report-template.json    # 리포트 데이터 템플릿
├── src/
│   ├── components/reports/
│   │   ├── MatchReport.astro       # 경기 리포트 컴포넌트
│   │   ├── CourseStatsSection.astro # 코스별 분석 컴포넌트
│   │   └── ...
│   ├── data/reports/
│   │   ├── reportData.js           # 데이터 관리 서비스
│   │   ├── YYYY-MM.json           # 월별 리포트 데이터
│   │   └── ...
│   └── pages/reports/
│       └── [period].astro         # 동적 라우팅 페이지
└── public/images/reports/
    └── YYYY-MM/
        └── gallery/               # 월별 갤러리 이미지
```

---

## 🎯 7. 매월 작업 순서

### 7.1 데이터 준비 (모임 직후)
1. **제공받은 파일 정리**
   - 성적표 Excel/CSV 파일
   - 조편성 이미지 또는 텍스트
   - 시상자 명단
   - 갤러리 사진들

2. **폴더 구성**
   ```
   tools/data/YYYY-MM/
   ├── scores.csv          # 성적 데이터
   ├── team-list.json      # 조편성 (변환 후)
   ├── awards.json         # 시상 정보
   └── gallery/            # 갤러리 이미지
   ```

### 7.2 자동 생성 실행
```bash
# 1. 리포트 생성 스크립트 실행
node tools/generate-report.js YYYY-MM

# 2. 생성 결과 확인
# - src/data/reports/YYYY-MM.json 생성됨
# - getStaticPaths에 새 period 추가됨
# - 갤러리 이미지 복사됨

# 3. 로컬 테스트
npm run dev

# 4. 빌드 및 배포
npm run build
npx gh-pages -d dist
```

### 7.3 품질 확인 체크리스트
- [ ] 모든 참가자 성적이 정확히 표시되는가?
- [ ] 코스별 분석이 홀별 데이터와 일치하는가?
- [ ] 경기 리포트 내용이 실제 경기와 일치하는가?
- [ ] 갤러리 이미지가 올바르게 로드되는가?
- [ ] 모바일에서도 잘 보이는가?

---

## 🚀 8. 향후 개선 계획

### 8.1 단기 개선 (1-2개월)
- OCR 통합으로 조편성 이미지 자동 파싱
- Excel 파일 직접 처리 (CSV 변환 불필요)
- 데이터 검증 자동화

### 8.2 중기 개선 (3-6개월)
- 웹 인터페이스로 데이터 입력 지원
- 실시간 집계 시스템
- 개인별 성적 추이 분석 추가

### 8.3 장기 개선 (6개월+)
- 홀별 상세 통계 섹션 추가
- AI 기반 경기 분석 및 추천
- 모바일 앱 연동

---

## 📊 9. 성과 측정

### 9.1 효율성 지표
- **작업 시간**: 기존 수동 작업 대비 90% 단축 목표
- **오류율**: 자동 계산으로 휴먼 에러 최소화
- **일관성**: 모든 월별 리포트 동일한 형식 보장

### 9.2 품질 지표
- **데이터 정확성**: 100% 계산 정확도 유지
- **사용자 만족도**: 리포트 조회수 및 체류 시간 향상
- **반응성**: 모든 디바이스에서 3초 내 로딩

---

## 💡 10. 핵심 포인트

### 10.1 데이터 계산 원칙
- **9홀 기준**: 파 36타가 기본, +오버파/-언더파로 실제 타수 계산
- **코스별 분석**: 전반/후반 사용자를 코스별로 통합하여 통계 산출
- **홀별 데이터 활용**: 향후 상세 분석을 위한 홀별 스코어 데이터 보존

### 10.2 스포츠 중계 스타일 원칙
- **생생한 현장감**: 실제 경기 상황을 스토리텔링으로 전달
- **데이터 기반 분석**: 객관적 수치를 바탕으로 한 흥미로운 해석
- **개성 있는 캐릭터**: 각 참가자의 특징과 강점을 부각

### 10.3 자동화 우선순위
1. **필수**: 코스별 통계 계산, 순위 정렬, 기본 통계
2. **권장**: 경기 리포트 자동 생성, 갤러리 정리
3. **선택**: OCR 조편성 파싱, 고급 분석

---

**결론**: 이 기획서를 바탕으로 매월 일관성 있고 흥미로운 리포트를 자동 생성하여, 청구회 동호회원들에게 더욱 풍성한 모임 경험을 제공할 수 있습니다.

**다음 액션**: 7월 모임 후 이 워크플로우를 실제 적용하여 프로세스 검증 및 개선점 도출