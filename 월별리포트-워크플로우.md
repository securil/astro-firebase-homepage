# 월별 리포트 워크플로우

**작성일**: 2025년 6월 27일  
**목적**: 월별 리포트 페이지 자동 생성 및 관리 시스템

---

## 📋 1. 입력 데이터 구조

### 1.1 제공받는 데이터
- **조편성 정보**: 이미지 또는 텍스트 파일
- **코스별/홀별 성적 데이터**: Excel 또는 CSV 파일
- **시상자 명단 및 내역**: 텍스트 또는 구조화된 데이터
- **모임 갤러리 폴더**: 이미지 파일들

### 1.2 데이터 표준화 형식

#### 조편성 정보 (JSON 형식으로 변환)
```json
{
  "title": "청구회 월례회 조편성 청구회(XXX회) YYYY년 MM월 DD일",
  "courses": [
    {
      "name": "몽블랑",
      "startTime": "12:29-12:44",
      "teams": [
        {
          "number": 1,
          "time": "12:29",
          "players": ["이름(기수)", "이름(기수)", ...]
        }
      ]
    }
  ]
}
```

#### 코스별 성적 데이터 (CSV → JSON)
```json
{
  "scores": [
    {
      "name": "홍길동",
      "generation": "30기",
      "course": "몽블랑/알프스",
      "frontNine": {
        "holes": [0, 1, 1, 2, 0, 1, 1, 1, 0], // 파 대비 스코어
        "score": 43, // 9홀 총 타수 (36 + 7오버)
        "courseName": "몽블랑"
      },
      "backNine": {
        "holes": [1, 0, 1, 1, 1, 0, 1, 1, 1],
        "score": 43,
        "courseName": "알프스"
      },
      "totalScore": 86
    }
  ]
}
```

---

## 🔄 2. 워크플로우 단계

### Step 1: 데이터 수집 및 검증
1. **조편성 이미지 → 텍스트 변환**
   - OCR 또는 수동 입력으로 구조화된 데이터 생성
   - 참가자 명단 및 기수 정보 추출
   - 코스별 팀 배정 정보 파싱

2. **성적 데이터 처리**
   - Excel/CSV 파일을 JSON으로 변환
   - 홀별 스코어를 파 대비 점수로 계산
   - 9홀 기준 36타 + 오버/언더파 = 실제 스코어

3. **시상 정보 구조화**
   - 시상 카테고리별 수상자 정리
   - 점수/기록 데이터 포함

### Step 2: 데이터 분석 및 집계
1. **참가자 통계**
   - 총 참가자 수 계산
   - 기수별/성별 분포 분석
   - 코스별 참가자 수 집계

2. **성적 분석**
   - 전체 평균 타수 계산
   - 코스별 상위 3명 추출
   - 타수 분포 분석 (70대, 80대, 90대, 100대+)
   - 언더파/80대 타수 인원 계산

3. **코스별 통계**
   ```javascript
   courseStats: [
     {
       name: "몽블랑",
       players: 24, // 전반+후반 참가자 수
       topPlayers: [
         { name: "홍길동", score: 76, generation: "30기" }
       ]
     }
   ]
   ```

### Step 3: 페이지 데이터 생성
1. **reportData 객체 생성**
   ```javascript
   const reportData = {
     'YYYY-MM': {
       title: '2025년 X월 정기모임 리포트',
       date: 'YYYY-MM-DD',
       location: '골프장명',
       participants: 48,
       avgScore: 87.4,
       bestScore: 74,
       underPar: 0,
       eighties: 27,
       weather: '맑음',
       temperature: '28°C',
       gallery: [...], // 갤러리 이미지 목록
       awards: [...], // 시상 내역
       courseStats: [...], // 코스별 통계
       scores: [...], // 전체 성적표
       femaleScores: [...], // 여성 성적표 (있는 경우)
       teamList: {...} // 조편성 정보
     }
   }
   ```

### Step 4: 파일 업데이트
1. **getStaticPaths 수정**
   ```javascript
   export async function getStaticPaths() {
     return [
       { params: { period: '2025-03' } },
       { params: { period: '2025-05' } },
       { params: { period: '2025-06' } },
       { params: { period: 'YYYY-MM' } } // 새 월 추가
     ];
   }
   ```

2. **갤러리 이미지 업로드**
   - `public/images/reports/YYYY-MM/` 폴더 생성
   - 갤러리 이미지들 업로드
   - 이미지 파일명 정규화

---

## 🛠️ 3. 자동화 도구 개발 계획

### 3.1 데이터 변환 스크립트
```javascript
// tools/generate-report.js
class ReportGenerator {
  constructor(period) {
    this.period = period; // 'YYYY-MM'
  }

  // Step 1: 조편성 데이터 파싱
  parseTeamList(teamListData) {
    // 조편성 이미지/텍스트를 JSON으로 변환
  }

  // Step 2: 성적 데이터 처리
  processScoreData(csvData) {
    // CSV 데이터를 구조화된 JSON으로 변환
    // 홀별 스코어 계산 (36 + 오버/언더파)
  }

  // Step 3: 통계 계산
  calculateStats(scores, teamList) {
    // 참가자 통계, 코스별 통계, 성적 분포 계산
  }

  // Step 4: reportData 생성
  generateReportData() {
    // 모든 데이터를 취합하여 최종 리포트 데이터 생성
  }
}
```

### 3.2 사용법
```bash
# 1. 데이터 파일들을 tools/data/YYYY-MM/ 폴더에 배치
# 2. 스크립트 실행
node tools/generate-report.js 2025-07

# 3. 자동으로 생성되는 결과물
# - src/pages/reports/[period].astro 파일 업데이트
# - public/images/reports/2025-07/ 갤러리 이미지 복사
```

---

## 📁 4. 폴더 구조

```
project/
├── tools/
│   ├── generate-report.js          # 리포트 생성 스크립트
│   ├── data/
│   │   └── YYYY-MM/
│   │       ├── team-list.json      # 조편성 정보
│   │       ├── scores.csv          # 성적 데이터
│   │       ├── awards.json         # 시상 정보
│   │       └── gallery/            # 갤러리 이미지들
│   └── templates/
│       └── report-template.json    # 리포트 데이터 템플릿
├── src/pages/reports/
│   └── [period].astro            # 동적 라우팅 페이지
└── public/images/reports/
    └── YYYY-MM/                  # 월별 갤러리 이미지
```

---

## 🔧 5. 구체적인 작업 순서

### 5.1 매월 리포트 생성 시
1. **데이터 준비**
   - `tools/data/YYYY-MM/` 폴더 생성
   - 조편성 정보를 `team-list.json`으로 변환
   - 성적 데이터를 `scores.csv`로 정리
   - 시상 정보를 `awards.json`으로 작성
   - 갤러리 이미지들을 `gallery/` 폴더에 배치

2. **스크립트 실행**
   ```bash
   node tools/generate-report.js YYYY-MM
   ```

3. **자동 처리 내용**
   - 성적 데이터 분석 및 통계 계산
   - 코스별 상위 3명 추출
   - 타수 분포 계산
   - reportData 객체 생성
   - `[period].astro` 파일 업데이트
   - getStaticPaths에 새 period 추가
   - 갤러리 이미지 복사

4. **빌드 및 배포**
   ```bash
   npm run build
   npx gh-pages -d dist
   ```

### 5.2 데이터 검증 체크리스트
- [ ] 참가자 수가 조편성표와 성적표에서 일치하는가?
- [ ] 코스별 참가자 분배가 정확한가?
- [ ] 홀별 스코어 계산이 정확한가? (36 + 오버파)
- [ ] 시상자 정보가 성적표와 일치하는가?
- [ ] 갤러리 이미지가 올바르게 표시되는가?

---

## 🚀 6. 향후 개선 계획

### 6.1 단기 개선
- **OCR 통합**: 조편성 이미지 자동 파싱
- **Excel 직접 처리**: CSV 변환 없이 Excel 파일 직접 읽기
- **데이터 검증**: 자동 데이터 무결성 검사

### 6.2 장기 개선
- **웹 인터페이스**: 브라우저에서 데이터 입력 및 리포트 생성
- **실시간 집계**: 성적 입력과 동시에 통계 자동 업데이트
- **모바일 최적화**: 스마트폰에서 데이터 입력 지원

---

## 📊 7. 성과 측정

### 7.1 효율성 지표
- **작업 시간 단축**: 기존 수동 작업 대비 90% 시간 절약
- **오류 감소**: 자동 계산으로 휴먼 에러 최소화
- **일관성**: 모든 월별 리포트의 동일한 형식 보장

### 7.2 품질 지표
- **데이터 정확성**: 100% 계산 정확도
- **디자인 일관성**: 템플릿 기반 통일된 UI/UX
- **반응성**: 모든 디바이스에서 최적화된 표시

---

## 💡 8. 실제 사용 예시

### 8.1 6월 리포트 데이터 구조 (실제 적용 예시)
```javascript
'2025-06': {
  title: '2025년 6월 정기모임 리포트',
  date: '2025-06-24',
  location: '이천 뉴스프링빌 CC',
  participants: 48,
  avgScore: 87.4,
  bestScore: 74,
  underPar: 0,
  eighties: 27,
  weather: '맑음',
  temperature: '28°C',
  
  // 코스별 통계 (수정된 정확한 데이터)
  courseStats: [
    {
      name: '올림프스',
      players: 24,
      topPlayers: [
        { name: '최치영', score: 74, generation: '36기' },
        { name: '박세환', score: 75, generation: '30기' },
        { name: '최관호', score: 80, generation: '27기' }
      ]
    },
    // ... 나머지 코스들
  ],
  
  // 조편성표 (추가된 실제 데이터)
  teamList: {
    title: '청구회 월례회 조편성 청구회(411회) 2025년 6월 24일 화요일',
    courses: [
      {
        name: '몽블랑',
        startTime: '3팀-12:29 분-12시44분',
        teams: [
          { 
            number: 1, 
            time: '12:29', 
            players: ['정세걸(30)', '정연삼(30)', '김정일(30)', '이인영(30)'] 
          }
          // ... 나머지 팀들
        ]
      }
      // ... 나머지 코스들
    ]
  }
}
```

### 8.2 성적 계산 로직 예시
```javascript
// 홀별 스코어 데이터 (파 대비)
const holeScores = [0, 1, 1, 1, 1, 1, 1, 2, 0]; // 전반 9홀
const par = 36; // 9홀 기준 파
const totalOverPar = holeScores.reduce((sum, score) => sum + score, 0); // 8오버
const nineHoleScore = par + totalOverPar; // 36 + 8 = 44타

// 18홀 총합
const frontNine = 44;
const backNine = 43;
const totalScore = frontNine + backNine; // 87타
```

---

**결론**: 이 워크플로우를 통해 월별 리포트 생성이 체계화되고 자동화되어, 빠르고 정확한 리포트 제작이 가능해집니다. 6월 리포트에서 실제 적용된 바와 같이, 조편성표 분석부터 성적 집계, 코스별 통계까지 모든 과정이 표준화되어 일관성 있는 리포트 생성이 가능합니다.